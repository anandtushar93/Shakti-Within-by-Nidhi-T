// EventDetailModal has been removed — the calendar now uses inline hover tooltips.
// This file is kept as an empty stub so that any legacy Git-committed imports do not break the build.
const EventDetailModal = () => null;
export default EventDetailModal;
