import React from 'react';
import CosmicCalendar from '../Calendar';

// ─── CTA Replacement Section (Cosmic & Astrology Calendar) ───────────────────
const CTA: React.FC = () => {
  return <CosmicCalendar />;
};

export default CTA;
